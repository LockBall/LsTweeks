# Dropdown Hover Arrow

## Dormant Graphics Follow-Up

- Dropdown hover indicators now use a small custom gold triangle from line
  textures in `functions/dropdown.lua`, but it looks worse than the native WoW
  dropdown arrow.

- Revisit later with a proper in-game asset or a better texture approach.

- Rejected attempts:
  - Text glyphs rendered as an empty box.
  - `Interface\Buttons\UI-SortArrow` was too thin and barely visible.
  - `Interface\ChatFrame\UI-ChatIcon-ScrollDown-Up` had the wrong visual shape.
